import { useLocation } from "react-router-dom"


export const BACKEND_URL = "http://localhost:3000"


 const host  = window.location.host;
export const CURRENT_URL = "http://" + host

//console.log(url)